import { EmptyState } from "@/components/EmptyState";

export default function SqlPage() {
  return (
    <EmptyState
      title="Console SQL"
      subtitle="Etapa 5 (inclui novo endpoint no backend)."
    />
  );
}
