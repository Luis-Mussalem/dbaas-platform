[pytest]
# Always run from backend/ (same convention as Alembic).
# pythonpath = . ensures 'src' and 'tests' are importable from backend/.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
