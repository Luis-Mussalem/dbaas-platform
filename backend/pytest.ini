[pytest]
# Rodar sempre a partir de backend/ (mesma convenção do Alembic).
# pythonpath = . garante que 'src' e 'tests' sejam importáveis a partir de backend/.
pythonpath = .
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
